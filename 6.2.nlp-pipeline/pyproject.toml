[project]
name = "textpipeline"
version = "0.1.0"
dependencies = [
    "nltk",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
